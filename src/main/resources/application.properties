# =============================================
#  E-COMMERCE MARKETPLACE - CONFIGURACIÓN
# =============================================

# --- SERVIDOR ---
server.port=8080
server.servlet.context-path=/api

# --- BASE DE DATOS SQL SERVER ---
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}

# --- JPA / HIBERNATE ---
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- JWT ---
app.jwt.secret=ZWNvbW1lcmNlbWFya2V0cGxhY2VjbGF2ZXNlY3JldGFzdXBlcnNlZ3VyYTIwMjQ=
app.jwt.expiration-ms=86400000
app.jwt.refresh-expiration-ms=604800000

# --- APP URL ---
app.url=http://localhost:5173

# --- CLOUDINARY (Imágenes) ---
cloudinary.cloud-name=dowwatxfe
cloudinary.api-key=853483779519766
cloudinary.api-secret=_yz64FARAO5bWXJIO-kxry8n7D8

# --- STRIPE ---
stripe.api-key=${STRIPE_API_KEY}
stripe.secret-key=${STRIPE_API_KEY}
stripe.webhook-secret=whsec_temporal_cambiar_despues

# --- MERCADOPAGO ---
mercadopago.access-token=TEST_TEMPORAL_CONFIGURAR_DESPUES
mercadopago.webhook-secret=whsec_temporal

# --- EMAIL (Gmail) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=TU_CORREO@gmail.com
spring.mail.password=TU_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- SWAGGER / OPENAPI ---
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# --- SUBIDA DE ARCHIVOS ---
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# --- LOGS ---
logging.level.com.ecommerce=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
